class News {
  // ignore: prefer_typing_uninitialized_variables
  var author, title, description, urlToImage, publishedAt;
  News({
    this.author,
    this.title,
    this.description,
    this.urlToImage,
  });
}
